============
Installation
============

At the command line::

    $ easy_install django-uploads-app

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-uploads-app
    $ pip install django-uploads-app
